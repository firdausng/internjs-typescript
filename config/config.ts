
export const env: { [key: string]: { [key: string]: string } } = {
    ui: {
        prod: "https://theintern.github.io/"
    }
};

export const find_timeout = 10000;

export const api_timeout = 5000;
